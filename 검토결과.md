# GGFC V3.17.3 점검 및 V3.18.1 수정 결과

검토 대상은 제공된 `GGFC_선수_DATA_Program_V3_17_3_Firebase_PlayerPhotoFix(1).html`입니다. V4.0이나 V3.13.8을 기준으로 되돌리지 않았습니다. 현재 버전의 능력치 계산식을 유지하고, 공유·권한·사진 입력·모바일 화면을 보완했습니다.

## 이번 비밀번호 버전의 변경

+ 로그인 화면의 Google 버튼과 UID 안내를 제거하고 비밀번호 입력칸 하나로 변경했습니다.
+ 프로그램 내부의 고정 로그인 ID를 사용합니다. Firebase 콘솔에 최초 한 번 비밀번호 사용자를 만들면 이후에는 비밀번호만 입력합니다.
+ 비밀번호 확인은 Firebase에서 처리하며 실제 비밀번호를 HTML·설정 파일·기록 JSON에 저장하지 않습니다.
+ 로그인 폼의 Enter 제출, 비밀번호 보기·숨기기, 취소·로그아웃 때 입력값 정리와 탭 세션 로그인을 적용했습니다.
+ 기존 경기 계산 코드의 변경은 로그인 이벤트 연결 부분뿐입니다.

## 확인한 문제와 수정

| 항목 | 원본에서 확인한 문제 | 수정본 처리 |
|---|---|---|
| 관리자 권한 | HTML에 고정 비밀번호가 있고, Firebase Authentication 없이 저장을 요청함 | 비밀번호 한 칸 + Firebase 비밀번호 인증 + 고정 관리자 로그인 ID를 검사하는 서버 규칙 사용 |
| Firebase 주소 | 제공된 설정에 `databaseURL` 없음 | 프로젝트 공개 설정을 반영하고 주소 입력 지점을 `firebase-config.js` 한 곳으로 통합 |
| 회원 화면 갱신 | `meta/savedAt` 변경을 콘솔에 출력할 뿐 실제 기록을 다시 읽거나 화면을 갱신하지 않음 | `ggfc/v3` 전체 값 변경을 구독하고 기록·설정·선수 카드 갱신 |
| 최초 표시 | 서버 응답 전에 로컬 기록 또는 샘플이 표시됨 | 빈 상태에서 시작하고 서버 기록을 수신. 샘플 자동 생성 제거 |
| 오래된 설정 | 로컬 대표승점 백업을 공유 데이터와 별도 우선순위로 복구 | 회원 화면은 서버의 동일한 설정 사용. 로컬 백업을 서버 설정 위에 자동 덮어쓰지 않음 |
| 저장 완료 판단 | 로컬 저장 성공과 공유 저장 성공을 구분하기 어려움 | 대기·완료·오류 상태를 표시하고 서버 응답 후 공유 저장 완료 알림 |
| 저장 충돌 | 전체 데이터를 두 경로에 별도 저장하여 일부 실패나 동시 덮어쓰기 가능 | 한 경로의 트랜잭션과 revision 검증으로 동시 수정 감지 |
| 변경 메타데이터 | 관리자 이력 함수가 정의되어 있지만 호출되지 않음 | 실제 저장 시 서버 시각·수정 UID·revision 기록. 완전한 변경 이력 시스템으로 안내하지 않음 |
| 로컬 저장공간 | 서버 데이터를 받은 후 localStorage 저장 실패가 화면 갱신을 중단시킬 수 있음 | 캐시 실패와 서버 수신·화면 갱신 분리 |
| 명단 병합 | Draft 시트마다 명단을 지워 복수 시트가 서로 덮일 수 있음 | 한 통합 파일 안의 Draft 시트를 모아서 병합 |
| 출석 선수의 연도 | 출석 행에 없는 `date`를 읽어 연도 없는 보조 명단이 생성됨 | 경기 ID로 실제 경기 날짜를 찾아 연도 적용 |
| 사진 선택 | 연도별 빈 경기 전용 행이 연도 없는 Draft 사진·포지션을 가릴 수 있음 | 실제 Draft 우선 선택과 사진 보완 |
| 사진 주소 입력 | 상대경로, GitHub 파일 페이지, 하이퍼링크의 표시명 처리 미흡 | 저장소 상대 이미지 주소·GitHub 링크 변환·엑셀 링크 추출 보완 |
| 사진 오류 처리 | 동적으로 만든 인라인 JavaScript에 이름을 끼워 넣음 | DOM 이미지 요소와 오류 이벤트 사용 |
| 파일 입력 실패 | JSON/엑셀 일부 처리 중 실패하면 잘못된 상태가 남을 수 있음 | JSON 형식 검사와 통합 엑셀의 일괄 적용·실패 시 복구 |
| Firebase 데이터 키 | 팀명 등에 Firebase에서 허용하지 않는 키 문자가 있으면 저장 오류 가능 | 문제가 되는 객체 키를 저장 시 인코딩, 읽을 때 복원 |
| 모바일 공간 | 작은 화면에도 좌측 사이드바가 146px를 차지하고 넓은 표가 남음 | 하단 5개 메뉴, 선수 검색·기록 카드, 능력치 6칸 카드, 모바일 선수 상세 화면 |
| 초기 계산량 | 화면 진입 때 여러 숨겨진 메뉴까지 전부 계산함 | 현재 메뉴만 계산하고 다른 메뉴는 열 때 갱신. 능력치 화면의 중복 계산 재사용 |

원본 HTML만으로 실제 Firebase에 배포된 규칙이 공개 쓰기를 허용했는지는 알 수 없습니다. 확인한 사실은 원본 클라이언트 코드가 서버가 검증할 관리자 로그인을 사용하지 않았다는 점입니다. 수정본도 **제공한 규칙을 실제 콘솔에 적용해야 서버 권한이 완성됩니다.**

## PC·모바일 화면

PC에서는 기존 선수 카드 왼쪽, 육각 그래프 가운데, 경기 기록 오른쪽 구성을 유지합니다. 모바일에서는 카드·육각 그래프·경기 기록을 세로로 배치합니다. 아래의 PAC·DRI·SHO·DEF·PAS·PHY 기간별 변화 그래프와 날짜 선택 기능도 유지합니다. 선수 능력치 표는 모바일에서 6개 능력치 카드로 보여 주며, 같은 계산 결과와 같은 조회 기준일을 사용합니다. 일부 상세 명단·원시 측정 표는 표 영역 안에서 가로 스크롤합니다.

## 검증 결과

- Node 실행 환경에서 빈 데이터 부팅, 관리자/열람 권한, 저장 성공·실패·재시도, 충돌, 연결 끊김, 서버 설정 수신, 사진 URL, JSON 검증, 복수 Draft, 명단 연도, 모바일 카드와 능력치 추이 등 **28개 시나리오 통과**.
- 제공한 규칙 파일의 표현식을 로컬 평가기로 검사하여 미인증·일반 회원 쓰기 차단, 관리자 쓰기 허용, revision·수정 UID·서버 시각 검증 등 **11개 검사 통과**. Firebase Emulator나 실제 서버에서 실행한 검사는 아닙니다.
- 기존 검증용 통합 기록의 **150경기, Draft 66명, 개인 득점 합계 461, 경기 스코어 합계 430** 보존.
- 같은 검증 기록을 V3.17.3과 수정본에 입력해 **전체 선수 집계와 능력치 대상 70명의 6개 능력치·OVR·라운드 기준 결과가 동일**함을 확인. 명단 외 경기 참여 선수도 능력치 대상에 포함되므로 Draft 인원과 다릅니다.
- JavaScript 문법, 두 HTML의 ID 중복 여부와 공유 파일 참조 검사 통과.

기존 검증용 기록에는 원래 경기 스코어와 개인 득점 합계가 맞지 않는 2026-04-24의 6경기가 있습니다. 원본 숫자를 임의로 바꾸지 않았고 기존 불일치 안내를 유지했습니다. 이는 이전에 보관된 검증용 기록에 대한 결과이며, 아직 접근하지 않은 현재 Firebase의 실제 기록을 점검했다는 뜻이 아닙니다.

## 남은 실제 환경 확인

현재 제공되지 않은 값은 **Realtime Database의 실제 URL**입니다. 관리자 UID를 수동 등록하는 절차는 필요하지 않습니다. Firebase에 비밀번호용 관리자를 만드는 초기 설정은 아직 진행하지 않았습니다. GitHub 저장소 주소도 아직 없습니다. 따라서 실제 서버 연결, 비밀번호 로그인, 게시된 규칙에 의한 차단, 두 기기의 실시간 반영은 현장 검증 전입니다. 모바일 CSS와 생성 구조를 점검했지만 실제 휴대폰 브라우저의 화면 렌더링·터치 동작·비밀번호 로그인·이미지 내보내기는 실행 검증하지 못했습니다.

배포 순서와 운영 방법은 `README_배포안내.md`에 정리했습니다. 오류가 전혀 없다고 보증하는 버전은 아니며, 위 검증을 통과한 배포 준비본입니다.

GitHub는 Pages를 비밀번호 전송 등 민감한 용도로 사용하는 것을 권장하지 않는다고 안내합니다. 관리자 화면의 실제 운영 위치는 이 제약을 함께 검토해야 하며, Pages 게시와 별도로 Firebase Hosting을 이용할 수 있습니다. [GitHub 공식 안내](https://docs.github.com/en/pages/getting-started-with-github-pages/securing-your-github-pages-site-with-https)
