# GGFC V3.18.13 — 종합기록 이미지 저장 개선

## GitHub에 반영할 파일

첨부해 주신 최종 V3.18.12 ZIP을 기준으로 수정했습니다. 압축을 푼 `ggfc-record-main` 폴더 안에서 아래 4개 파일을 기존 저장소의 같은 경로에 반영하세요.

| 경로 | 반영 방법 |
| --- | --- |
| `index.html` | 기존 파일 교체 |
| `mobile.html` | 기존 파일 교체 |
| `assets/ggfc-app.js` | 기존 파일 교체 |
| `assets/ggfc-export.css` | 새 파일 추가 |

1. ZIP을 압축 해제합니다.
2. 위 4개 파일을 GitHub 저장소에 반영합니다. `assets` 경로를 유지하세요.
3. 커밋하고 기존 배포가 끝나면 사이트를 새로고침합니다. PC에서 이전 버전이 보이면 Ctrl+Shift+R을 사용하세요.
4. 관리자 모드의 **종합기록 → 종합기록 이미지 저장**을 누릅니다.

이 ZIP은 업데이트된 전체 프로젝트를 포함합니다. ZIP 자체를 GitHub에 올리는 대신 압축을 푼 파일을 반영하세요. 이번 작업에서 실제 GitHub 저장소나 배포 사이트는 변경하지 않았습니다.

## 이미지 출력 변경

- PC·모바일 화면 크기와 무관하게 840px 폭의 출력 배치를 사용합니다.
- 기본 PNG 폭은 1680px입니다. 브라우저 메모리 부족 시 재시도 과정에서 해상도를 낮추되, 글씨의 배치 크기를 유지합니다.
- 표 본문 26px, 제목 30px, 경기 점수 36px를 기준으로 확대했습니다. 표 머리글·보조 정보는 20~24px입니다.
- 등록된 GGFC·리그·팀 로고를 크게 표시합니다. 로고가 없을 때는 기존 대체 표시를 사용합니다.
- 득점·출석·팀·개인·파울 순위를 세로로 배치합니다.
- ‘팀 순위 및 누적 기록’의 넓은 표는 팀별 카드로 바꾸며, 11개 기록값을 모두 유지합니다.
- 상대전적은 대진별 표로 바꾸며, 각 대진은 한 번씩 표시합니다. 승·무·패는 왼쪽 ‘기준 팀’의 결과입니다.
- 득점·출석·개인·파울 랭킹에는 실제 집계에 사용한 조회기간을 함께 표시합니다.
- 조회된 경기일의 상세 기록은 저장 이미지에서 펼쳐집니다.
- 경기 카드·팀 기록 카드·표 행이 중간에서 잘리지 않는 위치를 골라 나눕니다. 통상적인 랭킹 표는 제목과 열 이름이 함께 보이도록 한 이미지에 유지합니다.
- 짧은 출력은 PNG 1장, 긴 출력은 번호가 붙은 여러 PNG를 ZIP 1개로 저장합니다. 날짜·기록량에 따라 이미지 수가 달라집니다.
- 이미지가 여러 장일 때에는 상단에 이어지는 종합기록임을 표시하고 하단에 페이지 번호를 넣습니다.

## 네이버 카페에 올리기

예: `GGFC_종합기록_2026-05-23_5장.zip`이 저장되면 압축을 풀고 `01_…png`부터 `05_…png`까지 순서대로 첨부합니다. 이미지 자체를 다시 축소하거나 화면 캡처로 바꾸지 않고 생성된 PNG를 사용하세요.

상단 경기결과 날짜, 경기 상세 조회기간, 랭킹 조회기간은 기존 프로그램의 조회 기준을 따릅니다. 각 영역에 표시된 기간을 확인할 수 있도록 출력합니다.

## 검증 결과

- 로컬 Chromium 브라우저의 PC 1440px / 모바일 390px 화면에서 실제 저장 버튼 실행: 같은 크기·같은 내용의 PNG ZIP 생성.
- 샘플 1경기일과 누적 랭킹: 5장, 각 PNG 폭 1680px. 실제 운영 데이터에 따라 장수는 달라집니다.
- 팀 기록값 66개 및 상대전적 15개 대진을 기존 화면과 대조: 모두 일치.
- 30경기일 장기 조회: 24장으로 분할, 모든 경기일 포함, 보호 대상 카드·표 행을 가로지르는 분할 없음. 마지막 페이지 PNG 렌더링 확인.
- 빈 기록 화면: PNG 1장 저장 확인.
- 출력 실패 강제 발생: 저장 버튼 재활성화와 임시 출력 화면 제거 확인.
- 생성 ZIP의 압축 무결성 및 한글 파일명 확인.
- 출력 전후 원본 DB, 조회 결과와 접힘 상태 보존 확인. 원본 HTML 비교에서는 브라우저가 붙이는 빈 style 속성만 제외했습니다.
- 앱 코드의 이미지 출력 영역 밖 내용은 원본과 동일합니다. Firebase 설정·공유 동기화·데이터 규칙 파일도 원본과 동일합니다.

첨부 ZIP에는 실제 경기 데이터와 등록 로고가 포함되어 있지 않아 프로그램 내 샘플 데이터와 테스트 로고로 검증했습니다. 운영 서버 접속, 실제 관리자 로그인 및 네이버 카페 업로드는 이 검증에 포함되지 않습니다. 테스트 데이터·테스트 로고는 배포 파일에 넣지 않았습니다.
